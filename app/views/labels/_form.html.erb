<div class="form-group">
  <%= f.label :labels %>
  <%= f.text_feild :labels,
    class: 'form-control',
    placeholder: "Enter labels (separated by commas)",
    value: labels.map(&:name).join(', ') %>
</div>
